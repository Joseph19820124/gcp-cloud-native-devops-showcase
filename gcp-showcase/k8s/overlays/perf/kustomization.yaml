apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: showcase-perf

resources:
  - ../../base

patches:
  - path: replicas-patch.yaml
  - path: configmap-patch.yaml

# Image tags are set by the CI/CD pipeline at deploy time via:
#   kustomize edit set image backend=$BACKEND_IMAGE frontend=$FRONTEND_IMAGE
