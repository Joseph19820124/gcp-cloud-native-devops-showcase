# Demo placeholder only. In real environments this Secret should be generated
# by an External Secrets Operator / Sealed Secrets from AWS Secrets Manager,
# never committed to git in plaintext.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  DB_PASSWORD: "changeme"
